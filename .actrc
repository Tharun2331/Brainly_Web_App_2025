# Map runner labels to act's pre-built images
# catthehacker images are slim but have the most common tools pre-installed
-P ubuntu-24.04=catthehacker/ubuntu:act-24.04
-P ubuntu-latest=catthehacker/ubuntu:act-latest

# Don't pull images on every run (use cached version)
--pull=false
